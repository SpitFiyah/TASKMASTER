
  # TASKMASTER

  This is the TASKMASTER app, a gamified discipline and quest tracker with a retro terminal aesthetic.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  